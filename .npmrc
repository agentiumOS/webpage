# Hugeicons Pro private registry. The auth token is read from the
# HUGEICONS_TOKEN environment variable (Vercel project env, GitHub Actions
# secret, or your shell). Never commit the token itself.
@hugeicons-pro:registry=https://npm.hugeicons.com/
//npm.hugeicons.com/:_authToken=${HUGEICONS_TOKEN}
